import PlaceholderPage from "./PlaceholderPage";

const WebinarsPage = () => (
  <PlaceholderPage 
    title="Webinars" 
    description="On-demand webinars and upcoming live sessions about healthcare claims optimization."
  />
);

export default WebinarsPage;
