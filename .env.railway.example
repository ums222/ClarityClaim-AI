# =========================================
# RAILWAY DEPLOYMENT - Web Application
# =========================================
# Set these in Railway Dashboard → Variables
# These are for the WEB APP (app.apclaims.net)

# =========================================
# Frontend Variables (exposed to browser)
# =========================================

# Deployment mode - true for web app
VITE_IS_APP=true

# API URL - Railway serves both frontend and backend
VITE_API_URL=/api

# App URL - this Railway deployment
VITE_APP_URL=https://app.apclaims.net

# Marketing URL - points to Vercel for "Back to website" links
VITE_MARKETING_URL=https://www.apclaims.net

# Supabase (frontend - anon key)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...your-anon-key

# Stripe publishable key
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your-publishable-key

# =========================================
# Backend Variables (Express server)
# =========================================

# Server configuration
PORT=3001
NODE_ENV=production
FRONTEND_URL=https://app.apclaims.net

# Supabase service role (for backend API)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key

# Stripe (for subscriptions and payments)
STRIPE_SECRET_KEY=sk_live_your-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Google AI / Gemini (for AI features)
GOOGLE_AI_API_KEY=AIza...your-google-ai-key

# HubSpot CRM integration
HUBSPOT_ACCESS_TOKEN=pat-your-hubspot-token

# =========================================
# Railway Build Settings
# =========================================
# These are auto-configured by railway.json:
# Build Command: npm install && npm run build
# Start Command: npm run start:prod
# Health Check: /api/health
